- name: host file update - Local DNS setup across all the servers
    hosts: server
    tasks:
    - name: Add DR IP to etc hosts
      lineinfile:
        path: "/etc/hosts"
        line: "{{ item }}"
        state: present
        backup: yes
      with_items:
        - '10.200.1.202	ds1-db-01.ccc.digiserve.id'
        - '10.200.1.202	ds1-db-slave.ccc.digiserve.id'